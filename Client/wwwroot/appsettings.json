{
  "_comment": "BFF model: the client performs no OIDC and reads no auth config. All OIDC settings live on the Server (appsettings.json -> Oidc). The browser only uses the HttpOnly session cookie issued by the BFF."
}
